# 排除原始加密引擎（FileEncryptor.exe）
FileEncryptor.exe

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 虚拟环境与测试缓存
.venv/
.pytest_cache/

# 本地测试脚本（仅开发环境使用，不随仓库分发）
tests/

# 系统文件
Thumbs.db
.DS_Store

# 运行时生成的本地配置（窗口状态/主题/壁纸路径等，因人而异）
config.ini
# 引擎首次运行自动生成的运维配置（worker_threads 等）
fileencryptor.yaml